version: "3.8"

services:
  mongo:
    image: mongo:6
    container_name: dairy-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  backend:
    build: .
    container_name: dairy-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - mongo

volumes:
  mongo_data:
